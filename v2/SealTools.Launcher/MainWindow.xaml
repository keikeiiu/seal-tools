<ui:FluentWindow x:Class="SealTools.Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Seal Tools v2" Height="720" Width="920"
        WindowStartupLocation="Manual" Top="80" Left="120"
        ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ui:TitleBar Title="Seal Tools v2" Grid.Row="0"/>
        <Grid Grid.Row="1" Margin="20,12,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel x:Name="ToolsPanel"/>
            <TabControl x:Name="ConfigTabs" Grid.Row="1" Margin="0,16,0,0"
                        Background="{StaticResource BgBrush}" BorderBrush="{StaticResource CardBrush}"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
