<Window x:Class="SealTools.Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Seal Tools v2" Height="720" Width="920"
        Background="{StaticResource BgBrush}"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Seal Tools v2" Foreground="{StaticResource FgBrush}" FontSize="26" FontWeight="Bold"/>
        <StackPanel x:Name="ToolsPanel" Grid.Row="1" Margin="0,16,0,0"/>
        <TabControl x:Name="ConfigTabs" Grid.Row="2" Margin="0,16,0,0"
                    Background="{StaticResource BgBrush}" BorderBrush="{StaticResource CardBrush}"/>
    </Grid>
</Window>
