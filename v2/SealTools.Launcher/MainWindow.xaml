<ui:FluentWindow x:Class="SealTools.Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Seal Tools v2" Height="720" Width="920"
        WindowStartupLocation="Manual" Top="80" Left="120"
        ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ui:TitleBar Title="Seal Tools v2" Grid.Row="0"/>
        <Grid Grid.Row="1" Margin="20,12,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel x:Name="ToolsPanel"/>
            <!-- The configuration tabs are hidden unless you are actually configuring something: the
                 window opens as just the three tool cards, and the chevron reveals the rest. -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,14,0,0">
                <ui:Button x:Name="ConfigToggle" Appearance="Secondary" Padding="10,3,12,3"
                           ToolTip="Show or hide the configuration tabs"/>
                <ui:Button x:Name="PinToggle" Appearance="Secondary" Padding="10,3,12,3" Margin="8,0,0,0"
                           ToolTip="Keep this window above the game so the tool status stays visible while you play"/>
            </StackPanel>
            <TabControl x:Name="ConfigTabs" Grid.Row="2" Margin="0,10,0,0"
                        Visibility="Collapsed"
                        Background="{DynamicResource ApplicationBackgroundBrush}"
                        BorderBrush="{DynamicResource CardBackgroundFillColorDefaultBrush}"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
